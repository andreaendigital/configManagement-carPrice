#!/bin/bash
# Production startup script for systemd service

cd {{ app_dest }}

# Activate virtual environment
source venv/bin/activate

# Install dependencies silently if needed
pip install -e ".[dev]" --quiet --break-system-packages || pip install -e ".[dev]" --quiet

# Start backend API (port 5002)
cd backend
exec python3 app.py